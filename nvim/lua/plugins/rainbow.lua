return {
  {
    "HiPhish/rainbow-delimiters.nvim",
    event = "BufReadPost",
    config = function()
      -- This plugin usually works out of the box with default colors.
      -- You can customize it here if needed.
    end,
  },
}
